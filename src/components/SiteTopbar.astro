---
import IconMail from "@/assets/icons/IconMail.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconLanguage from "@/assets/icons/IconLanguage.svg";
import HomeTabs from "@/components/HomeTabs.astro";
import LinkButton from "@/components/LinkButton.astro";
import { SOCIALS } from "@/constants";
import { SITE } from "@/config";
import {
  getAlternateLocale,
  getLocaleFromPath,
  localizePath,
  ui,
} from "@/i18n";

type Props = {
  compactMobile?: boolean;
};

const { compactMobile = false } = Astro.props;

const locale = getLocaleFromPath(Astro.url.pathname);
const copy = ui[locale];
const alternateLocale = getAlternateLocale(locale);
const languageHref = localizePath(Astro.url.pathname, alternateLocale);
const emailLink = SOCIALS.find(social => social.href.startsWith("mailto:"));
const emailAddress = emailLink?.href.replace("mailto:", "");
const emailLabel = `${copy.nav.email} ${SITE.title}`;
---

<section
  class:list={[
    "app-layout lg:max-w-7xl lg:py-8",
    compactMobile ? "py-4" : "py-6",
  ]}
>
  <div
    class:list={[
      "border-b border-border/70 px-1 sm:px-2",
      compactMobile ? "pb-3" : "pb-4",
    ]}
  >
    <div
      class:list={[
        "flex flex-col lg:flex-row lg:items-center lg:justify-between",
        compactMobile ? "gap-3" : "gap-4",
      ]}
    >
      <div
        class:list={[
          "flex flex-col lg:flex-row lg:items-center lg:gap-10",
          compactMobile ? "gap-3" : "gap-5",
        ]}
      >
        <div
          class:list={[
            "min-w-0",
            compactMobile && "max-lg:flex max-lg:items-center max-lg:gap-3",
          ]}
        >
          <div
            class:list={[
              "overflow-hidden rounded-full border border-border/60 lg:hidden",
              compactMobile ? "h-10 w-10" : "mb-4 h-14 w-14",
            ]}
          >
            <img
              src="/avatar.avif"
              alt={`${SITE.author} avatar`}
              class="h-full w-full object-cover"
              loading="eager"
            />
          </div>
          <div class="min-w-0">
            <p
              class:list={[
                "[font-family:var(--font-ui-serif)] leading-none font-semibold tracking-tight",
                compactMobile ? "text-[1.6rem]" : "text-[1.85rem]",
              ]}
            >
              {SITE.author}
            </p>
            <p
              class:list={[
                "mt-2 text-[0.92rem] leading-6 text-foreground/56 lg:hidden",
                compactMobile && "hidden",
              ]}
            >
              {copy.site.desc}
            </p>
          </div>
        </div>

        <div
          class:list={[
            "flex items-center justify-between gap-4 lg:block",
            compactMobile && "max-lg:pt-1",
          ]}
        >
          <HomeTabs />

          <div class="flex items-center gap-1 lg:hidden">
            {
              emailLink && emailAddress && (
                <LinkButton
                  href={emailLink.href}
                  class="focus-outline flex size-10 items-center justify-center rounded-full text-foreground/68 transition hover:text-accent"
                  title={emailLabel}
                  aria-label={emailLabel}
                >
                  <IconMail class="size-5" />
                  <span class="sr-only">{copy.nav.email}</span>
                </LinkButton>
              )
            }

            <LinkButton
              href={localizePath("/search", locale)}
              class="focus-outline flex size-10 items-center justify-center rounded-full text-foreground/68 transition hover:text-accent"
              title={copy.nav.search}
              aria-label={copy.nav.search}
            >
              <IconSearch class="size-5" />
              <span class="sr-only">{copy.nav.search}</span>
            </LinkButton>

            <LinkButton
              href={languageHref}
              data-locale-target={alternateLocale}
              class="focus-outline flex size-10 items-center justify-center rounded-full text-foreground/68 transition hover:text-accent"
              title={copy.nav.languageLabel}
              aria-label={copy.nav.languageLabel}
            >
              <IconLanguage class="size-5" />
              <span class="sr-only">{copy.nav.languageLabel}</span>
            </LinkButton>

            {
              SITE.lightAndDarkMode && (
                <button
                  id="theme-btn"
                  class="focus-outline relative size-10 rounded-full text-foreground/68 transition hover:text-accent"
                  title={copy.nav.theme}
                  aria-label="auto"
                  aria-live="polite"
                >
                  <span
                    data-theme-icon-anchor
                    class="pointer-events-none absolute top-[50%] left-[50%] size-5 -translate-[50%]"
                    aria-hidden="true"
                  >
                    <IconMoon class="absolute inset-0 h-full w-full scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
                    <IconSunHigh class="absolute inset-0 h-full w-full scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
                  </span>
                </button>
              )
            }
          </div>
        </div>
      </div>

      <div class="hidden items-center gap-1 lg:flex lg:self-auto">
        <LinkButton
          href={localizePath("/search", locale)}
          class="focus-outline flex size-10 items-center justify-center rounded-full text-foreground/68 transition hover:text-accent"
          title={copy.nav.search}
          aria-label={copy.nav.search}
        >
          <IconSearch class="size-5" />
          <span class="sr-only">{copy.nav.search}</span>
        </LinkButton>

        <LinkButton
          href={languageHref}
          data-locale-target={alternateLocale}
          class="focus-outline flex size-10 items-center justify-center rounded-full text-foreground/68 transition hover:text-accent"
          title={copy.nav.languageLabel}
          aria-label={copy.nav.languageLabel}
        >
          <IconLanguage class="size-5" />
          <span class="sr-only">{copy.nav.languageLabel}</span>
        </LinkButton>

        {
          SITE.lightAndDarkMode && (
            <button
              id="theme-btn-desktop"
              class="focus-outline relative size-10 rounded-full text-foreground/68 transition hover:text-accent"
              title={copy.nav.theme}
              aria-label="auto"
              aria-live="polite"
            >
              <span
                data-theme-icon-anchor
                class="pointer-events-none absolute top-[50%] left-[50%] size-5 -translate-[50%]"
                aria-hidden="true"
              >
                <IconMoon class="absolute inset-0 h-full w-full scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
                <IconSunHigh class="absolute inset-0 h-full w-full scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
              </span>
            </button>
          )
        }
      </div>
    </div>
  </div>
</section>
